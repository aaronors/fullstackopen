import LoginForm from "./LoginForm";

const LoginDisplay = () => {
    return (
        <div>
            <h2>Log in to application</h2>
            <LoginForm />
        </div>
    );
};

export default LoginDisplay;
